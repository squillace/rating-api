project: "squillace/rating-api"
repository: "github.com/squillace/rating-api"
cloneURL: "https://github.com/squillace/rating-api.git"
# Used by GitHub to compute hooks.
# MAKE SURE YOU CHANGE THIS. It's basically a password.
sharedSecret: "<change this>"
# Use this to have Brigade update your project about the build.
# You probably want this if you want pull requests or commits to show
# the build status.
github:
  token: "<change this>"
secrets:
  acrServer: squillace.azurecr.io
  acrUsername: squillace
  acrPassword: "<change this>"
  slackWebhook: "<change this>"